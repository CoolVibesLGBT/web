import type { EditorThemeClasses } from 'lexical'

const StickyEditorTheme: EditorThemeClasses = {
  paragraph: 'sticky-paragraph',
  text: {
    bold: 'font-semibold',
    italic: 'italic',
    underline: 'underline',
    strikethrough: 'line-through',
  },
}

export default StickyEditorTheme
